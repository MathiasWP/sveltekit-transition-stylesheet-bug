# sveltekit transition stylesheet bug

1. Run npm i
2. Run npm dev
3. Open the dev-console in your browser
4. Toggle visibility of the element and you'll see that an empty stylesheet is appended everytime the transition finishes
